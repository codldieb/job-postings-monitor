# Secret used to protect the daily cron endpoint
CRON_SECRET=change-me-to-a-random-string

# Optional: base URL when calling the cron endpoint from an external scheduler
# APP_URL=http://localhost:3000
